import { Link } from "react-router-dom";
import { Card, CardContent, CardMedia, Typography } from "@mui/material";
import { demoThumbnailUrl, demoVideoUrl, demoVideoTitle, demo } from "../utils/constants";

const VideoCard = () => {
  return (
    <div>
      
    </div>
  )
}

export default VideoCard
